# The path to the page controller, which generates all the information for output.
abyss.page:
  path: '/abyss'
  defaults:
    _form:  '\Drupal\abyss\Form\AbyssEditModalForm'
    _title: 'Table for the report'
  requirements:
    _permission: 'access content'
